# TO-DO

## Переделать после 3й лабы:

- `com.example.workouttracker.data.lab_sync.NotificationHelper`
  - после создания планировщика тренировок будет высылать уведомление о них
- всё в папке provider
- package com.example.workouttracker.data.WorkoutRepository

## Экраны
- **Экран настройки** 
  - (добавить тему(ы?))
  - настройка уведомлений (регулярность напоминаний о залпнированных тренировках, типа за час, за полчаса, в 7:00 и др.)
  - словарь упражнений
- **Начать заготавливать экран статистики**
  - Блок статистики = ? продумать
- **Экран-планировщик тренировок** 
  - поле задания дня (календарь)
  - написать аналог ExerciseInputRow -> PremadeExerciseRow
    - `nameField` - `icon` - `weightField` - `SETS`
      - SET работает на boolean'е, `isDone`

## Логика
- **Подсчёт общего числа подходов к упражнению**
  - (SUM), ищем совпадения по всем `nameField`, суммируем все числа в input 
    - -> добавить проверку setInputField чтобы число было валидным `Int`
  - Добавить нарастающую прогрессию для будущего экрана статистики:
  ```
  forEach set in sets:
    name = { it.name }
    equipmentType = { it.equipmentType }
    forEach set in sets:
      if ({ it.name } == name && { it.EquipmentType } == equipmentType) {
        ...они связываются в единое упражнение для демонстрации на экране статистики,
        идёт учёт повышение веса (45-50-55-60 ...) и он рисует кривую, y = kg, x = reps
  
        собирается в какой-то файлик с данными потом в бдшке = ?
      }
  ```
- **После появления словаря в настройках:**
  - nameInputField будет давать подсказки по словарю (выпадающее меню?)
    - добавить regEx проверку опечаток

---
equipment и weight пока локальные (ExerciseInputRow.kt), позднее перевести во ViewModel
